#!/usr/bin/env python

from setuptools import setup, find_packages

setup(name='highway_custom',
      version='0.1',
      description='Custom highway environment.',
      packages=find_packages(),
     )